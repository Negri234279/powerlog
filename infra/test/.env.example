# TEST env — copy to infra/test/.env.
#   docker compose -f infra/test/compose.yml --env-file infra/test/.env up -d powerlog-postgres
NODE_ENV=test
APP_ENV=test

POSTGRES_USER=powerlog
POSTGRES_PASSWORD=powerlog
POSTGRES_DB=powerlog_test
POSTGRES_PORT=15434

API_PORT=4002
WEB_PORT=3002

JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
OTEL_SDK_DISABLED=true
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/graphql
