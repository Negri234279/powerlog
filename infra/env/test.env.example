# TEST environment — copy to infra/env/test.env.
# Mainly for a shared test DB / e2e. Unit + integration use Testcontainers.
NODE_ENV=test
APP_ENV=test

POSTGRES_USER=powerlog
POSTGRES_PASSWORD=powerlog
POSTGRES_DB=powerlog_test
POSTGRES_PORT=5434

API_PORT=4002
WEB_PORT=3002
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4002/graphql

JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
JWT_ISSUER=powerlog
JWT_EXPIRES_IN=15m
