# ── Alerting (powerlog's OWN Discord channel — distinct from core) ──
# Discord webhook for powerlog alerts (Server Settings → Integrations →
# Webhooks). Different channel/user from the pi-infra core webhook.
POWERLOG_DISCORD_WEBHOOK_URL=""