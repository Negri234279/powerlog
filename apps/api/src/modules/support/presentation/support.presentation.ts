import { ContactResolver } from './resolvers/contact.resolver'

/** GraphQL resolvers for the support module (admin resolver added in Block 2.2). */
export const SUPPORT_RESOLVERS = [ContactResolver]
