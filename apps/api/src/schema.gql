# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddExerciseEntryInput {
  exerciseId: ID!
  notes: String
  sessionId: ID!
}

type AdminCoachingStats {
  activeCoaches: Int!
  linkedAthletes: Int!
  links: Int!
  pendingInvitations: Int!
}

type AdminExercise {
  """
  squat | bench | deadlift | chest | back | shoulders | legs | arms | core
  """
  category: String!

  """barbell | dumbbell | machine | cable | bodyweight"""
  equipment: String!
  id: ID!
  name: String!

  """Spanish display name; null → English fallback."""
  nameEs: String

  """Primary muscle worked."""
  primaryMuscle: String!
  slug: String!
}

type AdminExercisePage {
  limit: Int!
  offset: Int!
  rows: [AdminExercise!]!

  """Total exercises matching the filter (ignoring pagination)."""
  total: Int!
}

type AdminUser {
  createdAt: DateTime!
  email: String!
  emailVerified: Boolean!
  id: ID!
  isAdmin: Boolean!

  """User role: "athlete" or "coach"."""
  role: String!

  """Account status: "active" | "disabled" | "deleted"."""
  status: String!

  """Public handle, or null if no profile yet."""
  username: String
}

type AdminUserPage {
  limit: Int!
  offset: Int!
  rows: [AdminUser!]!

  """Total users matching the filter (ignoring pagination)."""
  total: Int!
}

type AdminUserStats {
  active: Int!
  admins: Int!
  athletes: Int!
  coaches: Int!
  disabled: Int!
  newLast7Days: Int!
  newLast30Days: Int!
  total: Int!
  verified: Int!
}

type AdminWorkoutStats {
  activeUsers: Int!
  completedSessions: Int!
  exercises: Int!
  sessions: Int!
  sessionsLast7Days: Int!
  sets: Int!
}

type AiMesocycleDraft {
  createdAt: DateTime!

  """The template week."""
  days: [AiMesocycleDraftDay!]!
  goal: String
  id: ID!
  messages: [AiMesocycleDraftMessage!]!

  """The model that produced this proposal."""
  model: String!

  """The name the model proposed for the block."""
  name: String!

  """"openai" or "anthropic"."""
  provider: String!

  """"open", "accepted" or "discarded"."""
  status: String!

  """The 0–6 day offsets the athlete asked to train on."""
  trainingDays: [Int!]!
  updatedAt: DateTime!

  """Weeks the template is repeated into."""
  weeks: Int!
}

type AiMesocycleDraftDay {
  """0–6 offset from the week start."""
  dayOffset: Int!
  exercises: [AiMesocycleDraftExercise!]!
  label: String
}

type AiMesocycleDraftExercise {
  """The catalog exercise this resolved to."""
  exerciseId: ID!

  """The exercise’s canonical (English) name."""
  name: String!
  notes: String
  sets: [AiMesocycleDraftSet!]!
  slug: String!
}

type AiMesocycleDraftMessage {
  content: String!
  createdAt: DateTime!
  id: ID!

  """"user" or "assistant"."""
  role: String!
}

type AiMesocycleDraftSet {
  """The model’s one-line reason for this set."""
  notes: String

  """1-based position within the exercise."""
  order: Int!
  plannedReps: Int

  """Null where the athlete has no history to anchor a load on."""
  plannedWeightKg: Float
  rir: Int
  rpe: Float
}

type AiModel {
  displayName: String!
  id: String!
}

type AiPlanDraft {
  createdAt: DateTime!

  """The single exercise programmed; null → the whole session."""
  entryId: ID
  id: ID!
  messages: [AiPlanDraftMessage!]!

  """The model that produced this proposal."""
  model: String!

  """"openai" or "anthropic"."""
  provider: String!
  sessionId: ID!
  sets: [AiPlanDraftSet!]!

  """"open", "accepted" or "discarded"."""
  status: String!
  updatedAt: DateTime!
}

type AiPlanDraftMessage {
  content: String!
  createdAt: DateTime!
  id: ID!

  """"user" or "assistant"."""
  role: String!
}

type AiPlanDraftSet {
  """The exercise entry this set belongs to."""
  entryId: ID!

  """The model’s one-line reason for this set."""
  notes: String

  """1-based position within the entry."""
  order: Int!
  plannedReps: Int
  plannedWeightKg: Float
  rir: Int
  rpe: Float
}

type AiProviderConfig {
  createdAt: DateTime!

  """Whether powerlog may use this key."""
  enabled: Boolean!

  """The provider the AI features use when several are configured."""
  isDefault: Boolean!

  """Last four characters of the stored key."""
  keyLast4: String!

  """Selected model id; null → none picked yet."""
  model: String

  """"openai" or "anthropic"."""
  provider: String!
  updatedAt: DateTime!
}

type AiUsageRow {
  """Current USD per 1M input tokens; null → price unknown."""
  inputPricePerMTok: Float
  inputTokens: Float!
  lastUsedAt: DateTime!
  model: String!

  """Current USD per 1M output tokens; null → price unknown."""
  outputPricePerMTok: Float
  outputTokens: Float!

  """"openai" or "anthropic"."""
  provider: String!
  requests: Int!

  """Total cost so far; null → the model has no known price."""
  totalCost: Float
}

type AiUsageSummary {
  """ISO 4217 currency of every cost figure (USD)."""
  currency: String!

  """One line per (provider, model), most expensive first."""
  rows: [AiUsageRow!]!
  totals: AiUsageTotals!
}

type AiUsageTotals {
  inputTokens: Float!
  outputTokens: Float!
  requests: Int!
  totalCost: Float
}

input ChangePasswordInput {
  currentPassword: String
  newPassword: String!
}

type CoachAthleteNote {
  body: String!
  updatedAt: DateTime!
}

type CoachInvitation {
  """Null while the invited email has no account yet."""
  athleteId: ID
  coachId: ID!
  createdAt: DateTime!

  """The email the invitation was addressed to."""
  email: String!
  id: ID!

  """pending | accepted | declined | cancelled"""
  status: String!
}

type CoachInvitationPreview {
  """The inviting coach's handle."""
  coachUsername: String!

  """The invited email (prefilled + locked on signup)."""
  email: String!

  """A valid, available handle suggested from the email."""
  suggestedUsername: String!
}

type CoachUser {
  """Resolved avatar URL; null → show initials."""
  avatarUrl: String
  userId: ID!
  username: String!
}

input CreateExerciseInput {
  """
  squat | bench | deadlift | chest | back | shoulders | legs | arms | core
  """
  category: String!

  """barbell | dumbbell | machine | cable | bodyweight"""
  equipment: String!
  name: String!

  """Spanish display name (optional)."""
  nameEs: String

  """Primary muscle worked."""
  primaryMuscle: String!

  """Stable key; derived from name if omitted."""
  slug: String
}

input CreateSessionFromTemplateInput {
  notes: String

  """ISO 8601 datetime; defaults to now."""
  performedAt: String
  templateId: ID!
}

input CreateWorkoutSessionInput {
  notes: String

  """ISO 8601 datetime; defaults to now."""
  performedAt: String
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DistributionSlice {
  key: String!
  totalSets: Int!
  totalVolumeKg: Float!
}

type Exercise {
  """
  squat | bench | deadlift | chest | back | shoulders | legs | arms | core
  """
  category: String!

  """barbell | dumbbell | machine | cable | bodyweight"""
  equipment: String!
  id: ID!
  name: String!

  """Primary muscle worked."""
  primaryMuscle: String!
  slug: String!
}

type ExerciseEntry {
  exerciseId: ID!
  id: ID!
  notes: String
  order: Int!
  sets: [WorkoutSet!]!
}

type ExerciseHistorySet {
  """Estimated 1RM (kg) from the set (Epley)."""
  e1rmKg: Float

  """Programmed target reps, if any."""
  plannedReps: Int

  """Programmed target weight (kg), if any."""
  plannedWeightKg: Float
  reps: Int!
  rir: Int
  rpe: Float
  weightKg: Float!
}

type ExerciseSessionHistory {
  performedAt: DateTime!
  sessionId: ID!
  sets: [ExerciseHistorySet!]!

  """planned | completed"""
  status: String!
}

type ExerciseStats {
  """Best estimated 1RM (kg)."""
  bestE1rmKg: Float
  category: String!
  exerciseId: ID!

  """Heaviest single set (kg)."""
  heaviestWeightKg: Float
  name: String!
  slug: String!
  totalReps: Int!
  totalSets: Int!

  """Σ weight·reps (kg) over logged sets."""
  totalVolumeKg: Float!
}

input GenerateMesocycleDraftInput {
  """Free-text goal (e.g. hypertrophy, strength, peak)."""
  goal: String

  """
  Anything the model should know, e.g. "squat focus, no hack squat machine".
  """
  prompt: String

  """Days trained, as 0–6 offsets from the week start. Monday is 0."""
  trainingDays: [Int!]!

  """
  How many weeks the block runs for. The template week is repeated in each.
  """
  weeks: Int!
}

input GenerateMesocycleWeekInput {
  mesocycleId: ID!

  """Replace the week’s still-planned sessions if it exists."""
  replace: Boolean

  """1-based week to generate."""
  week: Int!

  """
  ISO date (YYYY-MM-DD) overriding the mesocycle start date as the anchor.
  """
  weekStartDate: String
}

input GenerateSessionPlanDraftInput {
  """Program only this exercise entry; omit for the whole session."""
  entryId: ID

  """
  Anything the model should know, e.g. "shoulder is sore, keep pressing light".
  """
  extraInfo: String
  sessionId: ID!
}

type IntensityBucket {
  sets: Int!

  """Rounded RPE (6–10) or RIR (0–50)."""
  value: Int!
}

input LogSetInput {
  entryId: ID!
  notes: String
  plannedReps: Int
  plannedWeight: Float
  reps: Int

  """Reps in reserve (alternative to RPE)."""
  rir: Int

  """RPE 0–10 in half-point steps."""
  rpe: Float
  sessionId: ID!

  """Weight unit of the inputs: kg | lb (default kg)."""
  unit: String
  weight: Float
}

input LoginInput {
  email: String!
  password: String!
}

type Me {
  createdAt: DateTime!
  email: String!

  """Whether the email address has been verified."""
  emailVerified: Boolean!

  """Whether the account has a password (vs Google-only)."""
  hasPassword: Boolean!
  id: ID!

  """Whether the user has admin privileges."""
  isAdmin: Boolean!

  """User role: "athlete" or "coach"."""
  role: String!

  """Unit preference: "kg" or "lb"."""
  units: String!

  """Public handle (unique)."""
  username: String!
}

type Mesocycle {
  createdAt: DateTime!

  """1-based weeks already generated into sessions."""
  generatedWeeks: [Int!]!
  goal: String
  id: ID!
  microcycles: [Microcycle!]!
  name: String!
  notes: String
  ownerId: ID!

  """
  Coach who plans this block for the owner (null = self-made). Only they can edit it.
  """
  plannedByUserId: ID

  """Anchor date of week 1 (unscheduled if null)."""
  startDate: DateTime

  """draft | active | completed | archived"""
  status: String!
  updatedAt: DateTime!
}

type MesocycleDayExercise {
  exerciseId: ID!
  id: ID!
  notes: String
  order: Int!
  sets: [MesocycleDaySet!]!
}

input MesocycleDayExerciseInput {
  exerciseId: ID!
  notes: String
  sets: [MesocycleDaySetInput!]!
}

type MesocycleDaySet {
  id: ID!
  notes: String
  order: Int!
  plannedReps: Int
  plannedWeightKg: Float
  rir: Int
  rpe: Float
}

input MesocycleDaySetInput {
  notes: String
  plannedReps: Int
  plannedWeight: Float

  """Target reps in reserve (alternative to RPE)."""
  rir: Int

  """Target RPE 0–10 in half-point steps."""
  rpe: Float

  """Weight unit of the inputs: kg | lb (default kg)."""
  unit: String
}

input MesocycleInput {
  """Free-text goal (e.g. hypertrophy, strength, peak)."""
  goal: String
  microcycles: [MicrocycleInput!]!
  name: String!
  notes: String

  """ISO date (YYYY-MM-DD) anchoring week 1."""
  startDate: String
}

type MesocycleSummary {
  dayCount: Int!
  goal: String
  id: ID!
  name: String!
  notes: String

  """Coach who plans this block for the owner (null = self-made)."""
  plannedByUserId: ID
  startDate: DateTime

  """draft | active | completed | archived"""
  status: String!
  updatedAt: DateTime!
  weekCount: Int!
}

type Microcycle {
  days: [MicrocycleDay!]!
  id: ID!
  label: String
  notes: String

  """1-based week position within the mesocycle."""
  weekIndex: Int!
}

type MicrocycleDay {
  """0–6 offset from the week start."""
  dayOffset: Int!
  exercises: [MesocycleDayExercise!]!
  id: ID!
  label: String
  notes: String
  order: Int!
}

input MicrocycleDayInput {
  """0–6 offset from the week start."""
  dayOffset: Int!
  exercises: [MesocycleDayExerciseInput!]!
  label: String
  notes: String
}

input MicrocycleInput {
  days: [MicrocycleDayInput!]!
  label: String
  notes: String
}

type Mutation {
  """Accept a pending invitation (links you to the coach)."""
  acceptInvitation(id: ID!): CoachInvitation!

  """
  Take the proposal into the builder. Writes nothing — `createMesocycle` still does that.
  """
  acceptMesocycleDraft(draftId: ID!): AiMesocycleDraft!

  """Write the draft’s targets onto its session."""
  acceptPlanDraft(draftId: ID!): AiPlanDraft!

  """Add an exercise to a session."""
  addExerciseEntry(input: AddExerciseEntryInput!): WorkoutSession!

  """
  Copy one of your own mesocycles to an athlete (they own the copy, you plan it; coaches only). The source stays in your library.
  """
  assignMesocycleToAthlete(athleteId: ID!, mesocycleId: ID!, startDate: String): Mesocycle!

  """
  Promote the current user to coach (re-issues the session with the new role).
  """
  becomeCoach: Me!

  """Cancel an invitation you sent (coaches only)."""
  cancelInvitation(id: ID!): CoachInvitation!

  """Change (or set, for Google-only accounts) the password."""
  changePassword(input: ChangePasswordInput!): Boolean!

  """Mark a session as completed."""
  completeWorkoutSession(id: ID!): WorkoutSession!

  """
  Build a mesocycle for one of your athletes: they own it, you plan it (coaches only).
  """
  createAthleteMesocycle(athleteId: ID!, input: MesocycleInput!): Mesocycle!

  """Create a catalog exercise."""
  createExercise(input: CreateExerciseInput!): Exercise!

  """Create a mesocycle (weeks → days → programmed sets)."""
  createMesocycle(input: MesocycleInput!): Mesocycle!

  """
  Create a session for yourself, pre-filled from one of your templates (status: planned).
  """
  createSessionFromTemplate(input: CreateSessionFromTemplateInput!): WorkoutSession!

  """Create a workout session (status: planned)."""
  createWorkoutSession(input: CreateWorkoutSessionInput): WorkoutSession!

  """Create a reusable workout template."""
  createWorkoutTemplate(input: WorkoutTemplateInput!): WorkoutTemplate!

  """Decline a pending invitation."""
  declineInvitation(id: ID!): CoachInvitation!

  """
  Permanently delete the current account (GDPR): soft-delete + scrub personal data.
  """
  deleteAccount: Boolean!

  """Forget the stored key for a provider. Idempotent."""
  deleteAiProviderKey(provider: String!): Boolean!

  """Delete a catalog exercise (blocked if used in any workout)."""
  deleteExercise(exerciseId: ID!): Boolean!

  """Delete a mesocycle (cascades to its weeks, days, exercises and sets)."""
  deleteMesocycle(id: ID!): Boolean!

  """Delete one notification, read or not (no-op if not the caller’s)."""
  deleteNotification(id: ID!): Boolean!

  """Delete every already-read notification; returns how many were cleared."""
  deleteReadNotifications: Int!

  """Delete a session (cascades to entries and sets)."""
  deleteWorkoutSession(id: ID!): Boolean!

  """Delete a template (cascades to its exercises and sets)."""
  deleteWorkoutTemplate(id: ID!): Boolean!

  """Throw away a draft."""
  discardMesocycleDraft(draftId: ID!): Boolean!

  """Throw away a draft without touching the session."""
  discardPlanDraft(draftId: ID!): Boolean!

  """
  Email a password-reset link (always succeeds, even if the email is unknown).
  """
  forgotPassword(email: String!): Boolean!

  """
  Ask the default AI provider to design a training block. Supersedes any open draft.
  """
  generateMesocycleDraft(input: GenerateMesocycleDraftInput!): AiMesocycleDraft!

  """
  Generate one week of a mesocycle into dated planned sessions (one per training day).
  """
  generateMesocycleWeek(input: GenerateMesocycleWeekInput!): [WorkoutSession!]!

  """
  Ask the default AI provider to program a planned session, or one exercise of it. Supersedes any open draft.
  """
  generateSessionPlanDraft(input: GenerateSessionPlanDraftInput!): AiPlanDraft!

  """
  Invite an athlete by email (coaches only). Works whether or not they have an account yet.
  """
  inviteAthlete(email: String!): CoachInvitation!

  """
  Leave one of your coaches. You keep everything they planned for you; they lose access to it.
  """
  leaveCoach(coachId: ID!): Boolean!

  """Append a set to an exercise entry."""
  logSet(input: LogSetInput!): WorkoutSession!

  """Log in with email + password."""
  login(input: LoginInput!): Me!

  """Revoke the refresh token and clear the auth cookies."""
  logout: Boolean!

  """Mark every notification read; returns how many changed."""
  markAllNotificationsRead: Int!

  """Mark one notification read (no-op if not the caller’s)."""
  markNotificationRead(id: ID!): Boolean!

  """
  Plan a session for an athlete from one of your templates (coaches only; status: planned).
  """
  planSessionFromTemplate(input: PlanSessionFromTemplateInput!): WorkoutSession!

  """
  Plan a session for one of your athletes (coaches only; status: planned).
  """
  planWorkoutSession(input: PlanWorkoutSessionInput!): WorkoutSession!

  """Ask the model to revise an open draft."""
  refineMesocycleDraft(input: RefineMesocycleDraftInput!): AiMesocycleDraft!

  """Ask the model to revise an open draft."""
  refinePlanDraft(input: RefinePlanDraftInput!): AiPlanDraft!

  """Rotate the session using the refresh cookie."""
  refresh: Me!

  """Register with email + password."""
  register(input: RegisterInput!): Me!

  """
  Stop coaching an athlete (coaches only). They keep everything you planned for them; you lose access to it.
  """
  removeAthlete(athleteId: ID!): Boolean!

  """Remove an exercise (and its sets) from a session."""
  removeExerciseEntry(entryId: ID!, sessionId: ID!): WorkoutSession!

  """Remove a set from an exercise entry."""
  removeSet(entryId: ID!, sessionId: ID!, setId: ID!): WorkoutSession!

  """Resend the verification email to the current user."""
  resendEmailVerification: Boolean!

  """Reset the password using the token from the reset email."""
  resetPassword(input: ResetPasswordInput!): Boolean!

  """Revoke every session except the current one."""
  revokeOtherSessions: Boolean!

  """Revoke one session by id (logs out that device)."""
  revokeSession(id: String!): Boolean!

  """
  Make this the provider the AI features use. Any previous default steps down.
  """
  setAiProviderDefault(provider: String!): AiProviderConfig!

  """Enable or disable a configured provider."""
  setAiProviderEnabled(input: SetAiProviderEnabledInput!): AiProviderConfig!

  """
  Store or replace the API key for a provider. Verified against the provider first.
  """
  setAiProviderKey(input: SetAiProviderKeyInput!): AiProviderConfig!

  """
  Set (or clear, when the body is empty) your private note on an athlete (coaches only).
  """
  setAthleteNote(athleteId: ID!, body: String!): Boolean!

  """Transition a mesocycle’s status (draft/active/completed/archived)."""
  setMesocycleStatus(id: ID!, status: String!): Mesocycle!

  """Grant or revoke platform admin (cannot revoke your own)."""
  setUserAdmin(input: SetUserAdminInput!): AdminUser!

  """Set a user's role (athlete ↔ coach)."""
  setUserRole(input: SetUserRoleInput!): AdminUser!

  """
  Disable (suspend) or re-enable a user account. Disabling signs them out.
  """
  setUserStatus(input: SetUserStatusInput!): AdminUser!

  """Select the model used for a configured provider."""
  updateAiProviderModel(input: UpdateAiProviderModelInput!): AiProviderConfig!

  """Edit a catalog exercise (slug is immutable)."""
  updateExercise(input: UpdateExerciseInput!): Exercise!

  """Replace a mesocycle’s details and its whole week/day/set tree."""
  updateMesocycle(id: ID!, input: MesocycleInput!): Mesocycle!

  """Update the authenticated user’s profile fields."""
  updateProfile(input: UpdateProfileInput!): Profile!

  """Edit a set (absent = leave, null = clear)."""
  updateSet(input: UpdateSetInput!): WorkoutSession!

  """
  Edit a session’s date and/or notes (absent = leave, notes null = clear).
  """
  updateWorkoutSession(input: UpdateWorkoutSessionInput!): WorkoutSession!

  """Replace a template’s name, notes and exercise/set tree."""
  updateWorkoutTemplate(id: ID!, input: WorkoutTemplateInput!): WorkoutTemplate!

  """Confirm email ownership with the token from the verification email."""
  verifyEmail(token: String!): Boolean!
}

type Notification {
  createdAt: DateTime!

  """Type-specific payload, JSON-encoded."""
  data: String!
  id: ID!

  """When the user read it; null while unread."""
  readAt: DateTime

  """Notification kind, e.g. "coach_invitation"."""
  type: String!
}

type NotificationsPage {
  hasNextPage: Boolean!
  items: [Notification!]!

  """Cursor for the next page; null when last."""
  nextCursor: String
}

type PendingInvitation {
  coachId: ID!

  """The inviting coach's username."""
  coachUsername: String!
  createdAt: DateTime!
  id: ID!
}

input PlanSessionFromTemplateInput {
  """The athlete this session is planned for."""
  athleteId: ID!
  notes: String

  """ISO 8601 datetime; defaults to now."""
  performedAt: String
  templateId: ID!
}

input PlanWorkoutSessionInput {
  """The athlete this session is planned for."""
  athleteId: ID!
  notes: String

  """ISO 8601 datetime; defaults to now."""
  performedAt: String
}

type Profile {
  """Avatar URL; null → client shows the default."""
  avatarUrl: String
  bio: String

  """Birth date as YYYY-MM-DD."""
  birthDate: String

  """ISO 3166-1 alpha-2 country code."""
  country: String
  createdAt: DateTime!
  displayName: String!
  firstName: String

  """Height in centimetres."""
  heightCm: Int
  lastName: String

  """BCP 47 locale, e.g. "es-ES"."""
  locale: String

  """"male" or "female"."""
  sex: String

  """IANA timezone, e.g. "Europe/Madrid"."""
  timezone: String
  updatedAt: DateTime!
  userId: ID!
}

type Query {
  """Aggregate coaching counts (admin only)."""
  adminCoachingStats: AdminCoachingStats!

  """
  Exercise catalog for admins (filter by category/equipment/muscle/text; offset-paginated).
  """
  adminExercises(categories: [String!], equipment: [String!], limit: Int, muscles: [String!], offset: Int, search: String): AdminExercisePage!

  """Aggregate user counts for the admin dashboard."""
  adminUserStats: AdminUserStats!

  """List users with filters + offset pagination (admin only)."""
  adminUsers(isAdmin: Boolean, limit: Int, offset: Int, roles: [String!], search: String, statuses: [String!], verified: Boolean): AdminUserPage!

  """Aggregate training counts (admin only)."""
  adminWorkoutStats: AdminWorkoutStats!

  """Models the stored key may call, fetched live from the provider."""
  aiModels(provider: String!): [AiModel!]!

  """The running API release (package version)."""
  apiVersion: String!

  """
  An athlete's per-exercise volume and PRs, optionally within a date range (coaches only).
  """
  athleteExerciseStats(athleteId: ID!, from: String, to: String): [ExerciseStats!]!

  """One of an athlete's mesocycles, with its full tree (coaches only)."""
  athleteMesocycle(athleteId: ID!, id: ID!): Mesocycle!

  """
  An athlete's mesocycles (newest first), with cheap rollups (coaches only).
  """
  athleteMesocycles(athleteId: ID!, search: String): [MesocycleSummary!]!

  """Your private note on one of your athletes (coaches only)."""
  athleteNote(athleteId: ID!): CoachAthleteNote

  """
  An athlete's e1RM progression and projection for one exercise (coaches only).
  """
  athleteStrengthProgression(athleteId: ID!, exerciseId: ID!, from: String, to: String): StrengthProgression!

  """
  An athlete's volume distribution (muscle/category) and RPE breakdown (coaches only).
  """
  athleteTrainingDistribution(athleteId: ID!, from: String, to: String): TrainingDistribution!

  """
  An athlete's headline training KPIs (incl. estimated S+B+D total), optionally ranged.
  """
  athleteTrainingSummary(athleteId: ID!, from: String, to: String): TrainingSummary!

  """
  An athlete's weekly training volume, optionally within a date range (coaches only).
  """
  athleteVolumeSeries(athleteId: ID!, from: String, to: String): [VolumeBucket!]!

  """
  An athlete's session history, newest first (same filters as your own history; coaches only).
  """
  athleteWorkoutHistory(athleteId: ID!, cursor: String, exerciseId: ID, from: String, limit: Int, query: String, status: String, to: String): WorkoutHistoryPage!

  """One of an athlete's sessions, with its full tree (coaches only)."""
  athleteWorkoutSession(athleteId: ID!, id: ID!): WorkoutSession!

  """
  Public: preview a pending invitation by its opaque token (for the invite-aware signup page).
  """
  coachInvitationPreview(token: String!): CoachInvitationPreview

  """
  The caller's recent completed sessions logging one exercise, with their performed sets.
  """
  exerciseSessionHistory(excludeSessionId: ID, exerciseId: ID!, limit: Int): [ExerciseSessionHistory!]!

  """
  The caller's per-exercise volume and PRs, optionally within a date range.
  """
  exerciseStats(from: String, to: String): [ExerciseStats!]!

  """The exercise catalog, optionally filtered by category."""
  exercises(category: String): [Exercise!]!

  """The currently authenticated user."""
  me: Me!

  """One of the caller’s mesocycles, with its full tree."""
  mesocycle(id: ID!): Mesocycle!

  """
  The training block awaiting a decision, if any. An athlete holds at most one.
  """
  mesocycleDraft: AiMesocycleDraft

  """
  The caller's mesocycles (newest first), with cheap rollups; optional name search.
  """
  mesocycles(search: String): [MesocycleSummary!]!

  """Providers the authenticated user has configured."""
  myAiSettings: [AiProviderConfig!]!

  """
  The caller's AI spend per model plus totals, estimated from a static price table.
  """
  myAiUsage: AiUsageSummary!

  """Athletes linked to the caller."""
  myAthletes: [CoachUser!]!

  """Coaches linked to the caller."""
  myCoaches: [CoachUser!]!

  """The caller's notification inbox, newest first (keyset-paginated)."""
  myNotifications(cursor: String, limit: Int): NotificationsPage!

  """The authenticated user's profile."""
  myProfile: Profile!

  """The user’s active sessions (devices)."""
  mySessions: [Session!]!

  """Pending invitations received by the caller."""
  pendingInvitations: [PendingInvitation!]!

  """Liveness ping for the GraphQL schema."""
  ping: String!

  """The proposal awaiting a decision on this session, if any."""
  sessionPlanDraft(sessionId: ID!): AiPlanDraft

  """
  e1RM progression and projection for one exercise, optionally within a date range.
  """
  strengthProgression(exerciseId: ID!, from: String, to: String): StrengthProgression!

  """
  The caller's volume distribution (muscle/category) and RPE breakdown, optionally ranged.
  """
  trainingDistribution(from: String, to: String): TrainingDistribution!

  """
  The caller's headline training KPIs (incl. estimated S+B+D total), optionally ranged.
  """
  trainingSummary(from: String, to: String): TrainingSummary!

  """Number of unread notifications (the bell badge)."""
  unreadNotificationsCount: Int!

  """The caller's weekly training volume, optionally within a date range."""
  volumeSeries(from: String, to: String): [VolumeBucket!]!

  """
  The caller's session history, newest first (keyset-paginated; optional status/date/exercise/notes filters).
  """
  workoutHistory(cursor: String, exerciseId: ID, from: String, limit: Int, query: String, status: String, to: String): WorkoutHistoryPage!

  """A workout session owned by the caller, with its full tree."""
  workoutSession(id: ID!): WorkoutSession!

  """One of the caller’s templates, with its full tree."""
  workoutTemplate(id: ID!): WorkoutTemplate!

  """
  The caller's templates (name-ordered), with cheap rollups; optional name search.
  """
  workoutTemplates(search: String): [WorkoutTemplateSummary!]!
}

input RefineMesocycleDraftInput {
  draftId: ID!

  """What to change, e.g. "swap the leg press for lunges"."""
  message: String!
}

input RefinePlanDraftInput {
  draftId: ID!

  """What to change, e.g. "less volume, I slept badly"."""
  message: String!
}

input RegisterInput {
  """Birth date as YYYY-MM-DD."""
  birthDate: String
  email: String!
  firstName: String

  """Height in centimetres (50–300)."""
  heightCm: Int
  lastName: String

  """BCP 47 UI locale, e.g. "es"."""
  locale: String
  password: String!

  """"kg" (default) or "lb"."""
  units: String

  """Public handle: 3–30 chars of a–z, 0–9 or underscore."""
  username: String!
}

input ResetPasswordInput {
  newPassword: String!
  token: String!
}

type Session {
  """Whether this is the session making the request."""
  current: Boolean!
  id: ID!
  ip: String

  """When this session was last used (last token rotation)."""
  lastUsedAt: DateTime!
  userAgent: String
}

input SetAiProviderEnabledInput {
  enabled: Boolean!

  """"openai" or "anthropic"."""
  provider: String!
}

input SetAiProviderKeyInput {
  """The provider API key. Stored encrypted; never returned."""
  apiKey: String!

  """Model to select; must be callable with this key."""
  model: String

  """"openai" or "anthropic"."""
  provider: String!
}

input SetUserAdminInput {
  isAdmin: Boolean!
  userId: ID!
}

input SetUserRoleInput {
  """"athlete" or "coach"."""
  role: String!
  userId: ID!
}

input SetUserStatusInput {
  """true → disable (suspend) the account; false → re-enable."""
  disabled: Boolean!
  userId: ID!
}

type StrengthPoint {
  e1rmKg: Float!
  performedAt: DateTime!
}

type StrengthProgression {
  points: [StrengthPoint!]!
  trend: StrengthTrend
}

type StrengthProjection {
  e1rmKg: Float!
  weeks: Int!
}

type StrengthTrend {
  projections: [StrengthProjection!]!

  """Fit quality R² in [0, 1]."""
  r2: Float!

  """kg gained per week (negative = regressing)."""
  slopePerWeekKg: Float!
}

input TemplateExerciseInput {
  exerciseId: ID!
  notes: String
  sets: [TemplateSetInput!]!
}

input TemplateSetInput {
  notes: String
  plannedReps: Int
  plannedWeight: Float

  """Target reps in reserve (alternative to RPE)."""
  rir: Int

  """Target RPE 0–10 in half-point steps."""
  rpe: Float

  """Weight unit of the inputs: kg | lb (default kg)."""
  unit: String
}

type TrainingDistribution {
  byCategory: [DistributionSlice!]!
  byMuscle: [DistributionSlice!]!
  rir: [IntensityBucket!]!
  rpe: [IntensityBucket!]!
}

type TrainingSummary {
  """Mean RPE over sets that recorded one."""
  avgRpe: Float
  bestBenchE1rmKg: Float
  bestDeadliftE1rmKg: Float
  bestSquatE1rmKg: Float
  distinctExercises: Int!

  """Estimated S+B+D total (kg)."""
  estimatedTotalKg: Float
  sessions: Int!
  totalReps: Int!
  totalSets: Int!

  """Σ weight·reps (kg) over logged sets."""
  totalVolumeKg: Float!

  """Distinct calendar days trained."""
  trainingDays: Int!
}

input UpdateAiProviderModelInput {
  """Model id, or null to clear the selection."""
  model: String

  """"openai" or "anthropic"."""
  provider: String!
}

input UpdateExerciseInput {
  category: String
  equipment: String
  exerciseId: ID!

  """Absent = leave unchanged."""
  name: String

  """
  Spanish display name. Empty string clears it; absent leaves it unchanged.
  """
  nameEs: String
  primaryMuscle: String
}

input UpdateProfileInput {
  bio: String

  """Birth date as YYYY-MM-DD."""
  birthDate: String

  """ISO 3166-1 alpha-2 country code."""
  country: String
  displayName: String
  firstName: String

  """Height in centimetres (50–300)."""
  heightCm: Int
  lastName: String

  """BCP 47 locale, e.g. "es-ES"."""
  locale: String

  """"male" or "female"."""
  sex: String

  """IANA timezone, e.g. "Europe/Madrid"."""
  timezone: String
}

input UpdateSetInput {
  entryId: ID!
  notes: String
  plannedReps: Int
  plannedWeight: Float
  reps: Int

  """Reps in reserve (alternative to RPE)."""
  rir: Int

  """RPE 0–10 in half-point steps."""
  rpe: Float
  sessionId: ID!
  setId: ID!

  """Weight unit of the inputs: kg | lb (default kg)."""
  unit: String
  weight: Float
}

input UpdateWorkoutSessionInput {
  """Absent = leave unchanged; null = clear."""
  notes: String

  """ISO 8601 datetime; absent = leave unchanged."""
  performedAt: String
  sessionId: ID!
}

type VolumeBucket {
  """Start of the week (UTC)."""
  bucketStart: DateTime!
  sessions: Int!
  totalSets: Int!
  totalVolumeKg: Float!
}

type WorkoutHistoryPage {
  hasNextPage: Boolean!
  items: [WorkoutSessionSummary!]!

  """Cursor for the next page; null when last."""
  nextCursor: String
}

type WorkoutSession {
  createdAt: DateTime!
  entries: [ExerciseEntry!]!
  id: ID!

  """Mesocycle this session was generated from, if any."""
  mesocycleId: ID

  """1-based mesocycle week this session belongs to, if any."""
  mesocycleWeek: Int
  notes: String
  performedAt: DateTime!

  """Coach who planned this session, if any."""
  plannedByUserId: ID

  """planned | completed"""
  status: String!
  updatedAt: DateTime!
  userId: ID!
}

type WorkoutSessionSummary {
  createdAt: DateTime!

  """Number of exercise entries."""
  exerciseCount: Int!
  id: ID!
  notes: String
  performedAt: DateTime!

  """Coach who planned this session, if any."""
  plannedByUserId: ID

  """Number of sets across all entries."""
  setCount: Int!

  """planned | completed"""
  status: String!

  """Σ weight·reps (kg) over logged sets."""
  totalVolumeKg: Float!
  updatedAt: DateTime!
  userId: ID!
}

type WorkoutSet {
  """Estimated 1RM (kg) from the actual set (Epley)."""
  e1rmKg: Float
  id: ID!
  notes: String
  order: Int!
  plannedReps: Int
  plannedWeightKg: Float
  reps: Int
  rir: Int
  rpe: Float
  weightKg: Float
}

type WorkoutTemplate {
  createdAt: DateTime!
  exercises: [WorkoutTemplateExercise!]!
  id: ID!
  name: String!
  notes: String
  ownerId: ID!
  updatedAt: DateTime!
}

type WorkoutTemplateExercise {
  exerciseId: ID!
  id: ID!
  notes: String
  order: Int!
  sets: [WorkoutTemplateSet!]!
}

input WorkoutTemplateInput {
  exercises: [TemplateExerciseInput!]!
  name: String!
  notes: String
}

type WorkoutTemplateSet {
  id: ID!
  notes: String
  order: Int!
  plannedReps: Int
  plannedWeightKg: Float
  rir: Int
  rpe: Float
}

type WorkoutTemplateSummary {
  exerciseCount: Int!
  id: ID!
  name: String!
  notes: String
  setCount: Int!
  updatedAt: DateTime!
}