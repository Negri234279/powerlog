import { ChatInboxView } from '@/components/chat/chat-inbox-view'

/** The unified chat inbox: all conversations (coaches + athletes) in one place. */
export default function ChatPage() {
    return <ChatInboxView />
}
